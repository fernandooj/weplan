//////////////////////////////////////////////////////////////////////
////////***********     llamo a las librerias        ****////////////
//////////////////////////////////////////////////////////////////////
let mongoose = require('mongoose')
let Schema   = mongoose.Schema;
let moment   = require('moment');


//////////////////////////////////////////////////////////////////////////////
////////***********     creo el esquema / titulo        ****//////////////
//////////////////////////////////////////////////////////////////////////////
let Titulo = new Schema({
	name: String,
	slug: String,
	autor: String,
	extension:String,
	genero:    {type: Schema.Types.ObjectId, ref:"Genero"},
	categoria: {type: Schema.ObjectId, ref:'Categoria'},
	usuario: {type: Schema.ObjectId, ref:'Usuario'},
	Created: { type: String, default: moment().format('YYYY-MM-DD h:mm:ss') },
})


//////////////////////////////////////////////////////////////////////////////
////////***********    exporto el esquema        ****/////////////////////////
//////////////////////////////////////////////////////////////////////////////

module.exports = mongoose.model('Titulo', Titulo)